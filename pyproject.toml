[project]
name = "pythonlibrary2mcp"
version = "1.0.0"
description = "Port Python library functions as MCP tools"
requires-python = "==3.12.*"
dependencies = [
    "fastmcp==2.8.1",
    "pytest>=8.4.1",
    "pyyaml>=6.0.2",
]
